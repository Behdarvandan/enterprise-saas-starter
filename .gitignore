# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js build output
/.next/
/out/

# production build output
/build

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local environment files
.env
.env*.local
.env.development
.env.production

# keep the committed environment template
!.env.example

# vercel deployment target
.vercel

# local Supabase CLI runtime state (supabase start / stop)
/supabase/.branches/
/supabase/.temp/

# typescript incremental build cache
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Sentry
.sentryclirc

repomix-output.xml
repomix-output.txt

# planning / scratch documents (not shipped with the app)
REFACTOR_PLAN.md
*_PLAN.md
*.draft.md

# agent / editor tool artifacts
.cline/
.clinerules
.clineignore
.remopix*
.vscode/
.idea/
# machine-local Claude Code settings (never shared/committed, per CLAUDE.md)
.claude/settings.local.json
CLAUDE.md
repomix-output.xml
repomix-output.txt
.cline/
.clinerules
.clineignore

# local Claude Code guidance (never shared/committed)
CLAUDE.local.md
.claude/
.context/
